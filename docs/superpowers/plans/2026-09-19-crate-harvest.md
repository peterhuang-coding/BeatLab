# 音乐下载与素材入库链路

用户直接授权：用 Coding Plan 完成当前链路里的音乐下载、爬虫。任务 ID `CRATE-20260919`，起点 `ed60482`，执行目录为 `.cache/album-execution-20260919`，分支 `codex/album-execution-20260919`。

## 设计与验收

沿用已验证的 Citizen DJ Blues / Jazz 官方公开 WAV 乐句，不引入付费下载或账户。选用固定清单的单进程批次，比每次增量扫描更便于恢复，也无需新增分布式队列。目录抓取保留权利声明原文与 SHA；下载通过有限重试、总截止时间、大小上限、完整音频校验与原子落盘；不同录音优先，下载后复用现有摄入事务。技术有效不表示片段好听。

增加 `crate` 命令，把发现、选取、下载、去重、入库与素材索引接通。每批次先固定所选 URL；失败后显式 `--resume` 继续同一清单，不偷偷换曲补足数量。只发现模式不下载音频。完成项核查原文件、规范化文件与数据库；损坏保留明确错误，避免覆盖既有素材。播放清单与来源表面向后续选句与制作。

1. Coding Plan 包 A：重构 `pipeline/connectors/citizen_dj.py`，保留 scan 兼容接口，新增 discover / cached_entry / fetch_entry；提供真实 WAV、HTTP 重试和边界测试。
2. Coding Plan 包 B：实现 `pipeline/crate.py` 与隔离 SQLite 测试，覆盖固定批次、全局数量上限、失败续跑、去重、损坏检测与独占锁。
3. 主代理接入 `pipeline/pipeline.py`，核验补丁与失败点；运行新测试及回归。命令不触发全库分离/评分，也不修改已有成品。
4. 使用隔离数据根真实发现和小批下载，连续恢复验证无重复下载；再向 2TB 实际素材库增量加入一批新录音，输出来源、原文件与规范化文件各自的哈希。
5. 记录测试、真实网络证据、委派采用情况、用法与局限，更新总计划/主线/Notion/Hub，普通 commit / push 并核验远端 SHA。

## 完成标准

- 一条命令得到可试听的 WAV、SQLite 资产/权利记录、固定批次 manifest、catalog.json、README 与 m3u8。
- 任一失败返回非零，成功项保留；恢复不改变选曲，不重复下载完整缓存，不重新处理全库。
- 真实下载、规范化和哈希验证通过；已有作品保持；Git 不含音频或 DB。
- 自动判断好句、完整歌曲下载、全平台爬虫、分离模型升级和自动编曲仍属后续范围。

## 状态

实现、实测与审阅完成：Coding Plan 起草下载器、批次模块、测试与使用说明，主代理按本地失败证据修正接口、锁释放和两层哈希等问题。99项 unittest 通过；隔离2段 + 正式8段真实下载入库；两批恢复均零网络请求且不重写音频，24份旧音频保持。新素材在正式库 `library/crates/old-records-20260919/`。详见 [使用说明](../../product/crate-harvest.md) 和 [真实证据](../../../evidence/crate-harvest-2026-09-19.json)。
